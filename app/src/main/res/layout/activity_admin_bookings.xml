<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/light_gray"
    android:orientation="vertical">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/primary_blue"
        android:orientation="vertical"
        android:padding="20dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnBack"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:text="Back"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:letterSpacing="0.01"
            android:paddingStart="0dp"
            app:icon="@drawable/ic_arrow_back_24"
            app:iconTint="@color/white"
            app:iconPadding="4dp"
            app:rippleColor="@color/primary_blue_dark"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ADMIN - All Bookings"
            android:textColor="#FFFFFF"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Scan member QR codes to manage gym entry"
            android:textColor="#AACDE8"
            android:textSize="13sp" />

    </LinearLayout>

    <!-- Scan Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:background="@color/white"
        android:elevation="2dp">

        <Button
            android:id="@+id/btnScanCheckIn"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:text="⛶ Scan Check-In"
            android:textSize="13sp"
            android:backgroundTint="#1A7F3C"
            android:textColor="#FFFFFF" />

        <Button
            android:id="@+id/btnScanCheckOut"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="⛶ Scan Check-Out"
            android:textSize="13sp"
            android:backgroundTint="#1565C0"
            android:textColor="#FFFFFF" />

    </LinearLayout>


    <!-- ── Date picker + status chips ──────────────────────────────────────── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:elevation="1dp"
        android:orientation="vertical"
        android:paddingHorizontal="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="6dp">

        <!-- Date row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="➤"
                android:textSize="16sp"
                android:layout_marginEnd="8dp"/>

            <TextView
                android:id="@+id/tvSelectedDate"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="All Dates"
                android:textSize="14sp"
                android:textColor="@color/dark_gray"
                android:textStyle="bold"/>

            <Button
                android:id="@+id/btnPickDate"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="Pick Date"
                android:textSize="12sp"
                android:backgroundTint="@color/primary_blue"
                android:textColor="@color/white"
                android:paddingHorizontal="14dp"/>

            <Button
                android:id="@+id/btnClearDate"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_marginStart="6dp"
                android:text="Clear"
                android:textSize="12sp"
                android:backgroundTint="@color/gray"
                android:textColor="@color/white"
                android:paddingHorizontal="10dp"/>

        </LinearLayout>

        <!-- Status chips -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none">

            <LinearLayout
                android:id="@+id/layoutStatusChips"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <!-- Chips injected in code — see AdminBookingsActivity.setupStatusChips() -->

            </LinearLayout>
        </HorizontalScrollView>

        <!-- Result count -->
        <TextView
            android:id="@+id/tvBookingCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="✦ bookings"
            android:textSize="11sp"
            android:textColor="@color/gray"/>

    </LinearLayout>

    <!-- Column Headers -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:background="@color/white"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1.2"
            android:gravity="center_vertical"
            android:text="ID / USER"
            android:textColor="@color/dark_gray"
            android:textSize="11sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:gravity="center_vertical"
            android:text="WORKOUT / DATE"
            android:textColor="@color/dark_gray"
            android:textSize="11sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_vertical|end"
            android:text="STATUS"
            android:textColor="@color/dark_gray"
            android:textSize="11sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- Empty State -->
    <TextView
        android:id="@+id/tvAdminEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="64dp"
        android:gravity="center"
        android:text="No bookings in the system yet."
        android:textColor="@color/gray"
        android:textSize="15sp"
        android:visibility="gone" />

    <!-- Booking List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerAdminBookings"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        android:paddingBottom="16dp" />

</LinearLayout>